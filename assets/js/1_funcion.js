// modificar esta funcion de declaracion a funcion de expresion
//function example(a, b, c){
   // return a+b+c
   // } 


const example = function(a, b, c) {
    return a + b + c
};

// Ver el resultado en la consola 
console.log(example(1, 2, 3)); // Deberia mostrar en la consola 6